{
    location(location: {id:1}) {
        barcode
        id
        description
        address
        size { numRows, numColumns}
        parent { id }
        children { id }
        stored { barcode }
        direction
    }
}