{
    location(location: {id:1}) {
        barcode
        id
        name
        description
        address
        size { numRows, numColumns}
        parent { id }
        children { id }
        stored { barcode }
        direction
    }
}